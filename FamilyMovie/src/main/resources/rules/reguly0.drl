package com.sample

import static com.sample.FamilyMovieMain.*;
import java.util.ArrayList;

declare Activity
	question: String @key
	respond: String
end

declare permissionToNextWindow

end

rule "Init"
	when
	then
		String questionContent = "What type of movie do they feel like watching?";
		ArrayList<String> options = new ArrayList<>();
		options.add("Take us on an adventure!");
		options.add("We love to laugh!");
		options.add("We're big sports fans!");
		options.add("We have a soft spot for animation!");
		options.add("We like to sign and dance!");
		options.add("We're animal lovers!");
		options.add("We want to be wowed!");
		
		//String[] options = {option1, option2, option3, option4, option5, option6, option7};
		Activity a = new Activity(questionContent);
		a.setRespond(options.get(multiplicityQuestion(a.getQuestion(), options)));
		//a.setRespond(options.get(questionBoard(a.getQuestion(), options)));
		insert(a);
		
end

rule "Take us on an adventure!"
	when 
		Activity(question == "What type of movie do they feel like watching?", respond=="Take us on an adventure!")
	then
		respondBoard("Wybra³eœ Take us on an adventure!");
end

rule "We love to laugh!"
	when 
		Activity(question == "What type of movie do they feel like watching?", respond=="We love to laugh!")
	then
		respondBoard("Wybra³eœ We love to laugh!");
end

rule "We're big sports fans!"
	when 
		Activity(question == "What type of movie do they feel like watching?", respond=="We're big sports fans!")
	then
		respondBoard("Wybra³eœ We're big sports fans!");
end

rule "We have a soft spot for animation!"
	when 
		Activity(question == "What type of movie do they feel like watching?", respond=="We have a soft spot for animation!")
	then
		respondBoard("Wybra³eœ We have a soft spot for animation!");
end

rule "We like to sign and dance!"
	when 
		Activity(question == "What type of movie do they feel like watching?", respond=="We like to sign and dance!")
	then
		respondBoard("Wybra³eœ We like to sign and dance!");
end

rule "We're animal lovers!"
	when 
		Activity(question == "What type of movie do they feel like watching?", respond=="We're animal lovers!")
	then
		respondBoard("Wybra³eœ We're animal lovers!");
end

rule "We want to be wowed!"
	when 
		Activity(question == "What type of movie do they feel like watching?", respond=="We want to be wowed!")
	then
		respondBoard("Wybra³eœ We want to be wowed!");
end
